# MagazineStoreClientApp

* Checkout the code from the reposiotry
* Restore the nuget packages or build the source(it will restore the packages automatically.)
* Run the application 
* The result will be displayed in the console. 
